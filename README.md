# Orbit

Saaqib's work command center. Every client, every date, every next move, plus a weekly Friday pack.

See `CLAUDE.md` for how the project is put together and `docs/orbit-log.md` for decisions and status.

```bash
pnpm install
cp .env.example .env.local   # fill in values
pnpm dev
```
